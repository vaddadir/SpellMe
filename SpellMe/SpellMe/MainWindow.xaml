<Window x:Class="SpellMe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SpellMe.ViewModels"
        Title="Spell Me" Height="350" Width="525" DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=Main}">
   <Grid>
        <Button Name="SayIt" Content="{Binding SayItText}"></Button>     
   </Grid>
</Window>
